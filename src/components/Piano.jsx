
import { PIANO_SMALL, PIANO_MEDIUM, PIANO_LARGE, PIANO_SIZE_MAP } from './constants.js';

const WhiteKey = (props) => {
  const { width = '50px' } = props;
  return (
    <div
      style={{borderRight: '1px solid black', width }}
      onClick={() => {}}
    />
  );
};

export const Piano = (props) => {
  return (
    <div>

    </div>  
  );
};

export default Piano;